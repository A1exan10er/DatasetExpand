# Flickr API key can be created here https://www.flickr.com/services/apps/create/apply

Store the personal API key in a separate file "api_keys.py" with following content:

flickr_keys = {
    "key": "put-api-key-here",
    "secret": "put-secret-here"
}